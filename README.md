# Dreamer

Notes. Canvas. Calendar. Self-hosted.

## Run

**Docker:**
```bash
docker compose up -d --build
```

**Manual:**
```bash
npm run install:all
npm run build
npm start
```

Open `http://localhost:3000`

---

*Finally, a note app you can only dream of.*
